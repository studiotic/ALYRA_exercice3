/*
const value = await votingContract.methods.owner().call({ from: account });

const owner = await votingContract.methods.owner().call();

.then(ownerAddr => {
                console.log("ownerAddr", ownerAddr);
                return ownerAddr;
            });
            */
